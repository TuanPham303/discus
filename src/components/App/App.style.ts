import theme from '../../style';

const appStyle = {
  wrapper: {
    height: '100vh',
    width: '100vw',
    display: 'flex',
    flexDirection: 'column',
    background: theme.color.background,
  }
}

export default appStyle;
