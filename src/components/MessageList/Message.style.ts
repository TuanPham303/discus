import theme from '../../style';

const messageStyle = {
  wrapper: {
    display: 'flex',
    color: theme.color.white,
  },
  message: {
    wordBreak: 'break-word',
    padding: 10,
  },
  userName: {
    padding: 10,
  }
}

export default messageStyle;
